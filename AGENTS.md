# Agents

Instructions for coding agents working in this repo live in [CLAUDE.md](CLAUDE.md). Read it first; it covers the design constraints, the dev loop (QML edits need `omarchy restart shell`), the index cache rules, and what must never be committed.
